node_modules
.next
.env.local
*.log
.DS_Store
data/
tsconfig.tsbuildinfo
.claude/settings.local.json
# Estado local das ferramentas de agente — por maquina, nao do projeto.
.claude/proven-config.json
.claude/.proven-config-version
.claude-flow/
.env.local.cloud-backup
.env.local.backup-*

# --- self-hosted stack (selfhost/) ---
# Secrets and per-machine state. Generated by selfhost/scripts/install.js.
selfhost/docker/.env
selfhost/docker/.env.old
selfhost/config.json
# Live data: Postgres lives in volumes/db/data (covered by `data/` above),
# uploaded invoice files live here.
selfhost/docker/volumes/storage/
# Generated per install: the root of Caddy's internal CA, and the backups.
selfhost/server/root-ca.crt
selfhost/backups/
.test-build
